/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'new-backend-server-production.up.railway.app', '89.116.20.142' ]
    }
};

export default nextConfig;
